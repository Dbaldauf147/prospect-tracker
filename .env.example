# ---------------------------------------------------------------------------
# Prospect Tracker — environment variables
#
# Copy this file to `.env` (gitignored) for local dev, and set the same keys
# in your Vercel project settings for production. Variables prefixed with
# VITE_ are exposed to the browser bundle; everything else is server-only.
# ---------------------------------------------------------------------------

# === Access control (multi-user) ==========================================
# Comma-separated list of email domains allowed to sign in / register.
# The client (VITE_) and the server value MUST match, and both MUST match
# the allowedDomains() list in firestore.rules. Leave empty to fail closed
# (only the admin email can sign in).
VITE_ALLOWED_EMAIL_DOMAINS=example.com,acme.com
ALLOWED_EMAIL_DOMAINS=example.com,acme.com

# === Firebase (client) =====================================================
# From Firebase console > Project settings > Your apps > SDK config.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# === Firebase Admin (server) ===============================================
# Service-account JSON used to verify caller ID tokens on every API route.
# Paste the JSON on one line, or base64-encode it. Generate one at
# Firebase console > Project settings > Service accounts > Generate new key.
#
# Also used by scripts/deployFirestoreRules.mjs, which publishes
# firestore.rules during the Vercel production build (and on
# `npm run deploy:rules`). That needs the "Firebase Rules Admin" role
# (roles/firebaserules.admin) on top of what the API routes need; without
# it the build logs the refusal by name and deploys the site anyway, with
# the rules left as they were.
FIREBASE_SERVICE_ACCOUNT_KEY=

# Separate service-account credential used only by the Power Automate
# calendar webhook (api/calendar-webhook.js). May be the same value as
# FIREBASE_SERVICE_ACCOUNT_KEY.
FIREBASE_SERVICE_ACCOUNT=

# === API origin / CORS =====================================================
# Production URL of the deployed app. API routes only accept cross-origin
# requests from this origin. e.g. https://prospect-tracker.vercel.app
APP_ORIGIN=

# Origin used to build email open-pixel / click-redirect links. Leave
# blank to reuse APP_ORIGIN (the default). Set only if you want tracking
# links served from a different domain than the app.
TRACKING_BASE_URL=

# === Shared, auth-gated third-party keys ===================================
# These remain a single shared credential; API routes require a verified
# signed-in user and apply per-user rate limiting before using them.
ANTHROPIC_API_KEY=
# How long the Claude web-search research routes (revenue / compliance on
# the Corporate Compliance page) may run before they abort and return a
# clean 504. Must stay under the deployment's own serverless function
# limit — Vercel Hobby caps Node functions at 60s regardless of the
# maxDuration in vercel.json, so leaving this blank uses a 50s budget that
# fits there. On a plan that allows longer functions, raise it (e.g.
# 290000 to use the 300s vercel.json asks for).
RESEARCH_TIMEOUT_MS=
APOLLO_API_KEY=
HUBSPOT_ACCESS_TOKEN=
RESEND_API_KEY=

# Granola — the AI notetaker the Call Recordings page ingests calls from.
# Create the key in Granola under Settings > API; it needs note AND
# transcript read access, which is a Business or Enterprise plan (keys
# look like grn_...). Without it the Call Recordings page still works
# from OneDrive / a local folder, and its Granola tab explains what to
# set. Granola only serves notes it has finished summarising, so a call
# that just ended appears on the next sync rather than immediately.
GRANOLA_API_KEY=
# NOT the key — this is the API's address, and it only exists so the
# integration can be pointed elsewhere if Granola moves the public API
# off its default host. Leave it blank. A value that isn't an http(s)
# URL is ignored, so a key pasted here by mistake breaks nothing, but it
# also does nothing.
GRANOLA_API_BASE=

# Gmail SMTP for the scheduled PE Opps email digest. Uses a Gmail account
# plus an App Password so the file can be emailed to any recipient for free
# (no paid sending domain). To create the App Password: enable 2-Step
# Verification on the Google account, then visit
# https://myaccount.google.com/apppasswords and generate one. Paste the
# 16-character code (spaces optional) as GMAIL_APP_PASSWORD.
GMAIL_USER=
GMAIL_APP_PASSWORD=
# Optional display name shown as the sender. Defaults to "Prospect Tracker".
GMAIL_FROM_NAME=

# --- Trade newsletters as a deal source -----------------------------------
# The weekly acquisition-news digest reads the PE trade newsletters out of
# the same Gmail account it sends from, because a lot of PE deal flow lands
# in PE Hub Wire and Axios Pro Rata days before it reaches the open web,
# and some of it never reaches the open web at all. No extra credential is
# needed: GMAIL_APP_PASSWORD above unlocks IMAP as well as SMTP.
#
# To turn it on, in the GMAIL_USER mailbox:
#   1. Subscribe that address to the newsletters (or auto-forward them to
#      it from wherever you read them today).
#   2. Create a label named below, and a Gmail filter per newsletter that
#      applies it. "from:(pehub.com OR axios.com)" covers both, and
#      "Skip the Inbox" keeps them out of your way.
#   3. Settings > Forwarding and POP/IMAP > Enable IMAP.
# Then press "Send test now" on the schedule: the result says how many
# newsletter headlines it read, so you can tell the wiring worked.
#
# Two gates stand between this and the rest of the mailbox: it opens only
# this one label, never INBOX, and inside the label it reads only mail from
# the senders allow-listed below.

# The label the newsletters are filtered into. Defaults to "deal-news".
NEWSLETTER_LABEL=

# Sender domains that may be read inside that label, comma-separated.
# Leave blank for the default set: pehub.com, axios.com, buyoutsinsider.com,
# peimedia.com. Subdomains are matched too, so a newsletter sent from
# email.pehub.com is covered by pehub.com.
NEWSLETTER_SENDERS=

# Secret shared with Vercel Cron. When set, the PE Opps scheduler
# (api/pe-opps-scheduler.js) rejects any request whose
# Authorization: Bearer <token> doesn't match. Vercel attaches this
# header automatically to cron invocations when CRON_SECRET is set.
CRON_SECRET=

# Google Sheets sync service-account JSON (one line or base64).
GOOGLE_SERVICE_ACCOUNT_KEY=

# === Microsoft / Outlook OAuth =============================================
OUTLOOK_CLIENT_ID=
OUTLOOK_CLIENT_SECRET=
OUTLOOK_REDIRECT_URI=
